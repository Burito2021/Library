spring:
  security:
    user:
      name: admin
      password: admin123

  profiles:
    active: local
  application:
    name: library

info:
  app:
    version: 1.0.0

jasypt:
  encryptor:
    password: mySecretKey
#second way of implementation
#server:
#  servlet:
#    context-path: /api/v1/

banner:
  time-format: yyyy-MM-dd HH:mm:ss
  current-time: ${current.time}

jwt:
  secret: "myVeryLongSecretKeyThatIsAtLeast32CharactersLongForSecureJWTTokenGeneration"
  access-token:
    expiration: 1 # 15 minutes
  refresh-token:
    expiration: 2  # 60 minutes
