spring:
  security:
    user:
      name: admin
      password: admin123

  profiles:
    active: local
  application:
    name: library

info:
  app:
    version: 1.0.0

jasypt:
  encryptor:
    password: secret
#second way of implementation
#server:
#  servlet:
#    context-path: /api/v1/

banner:
  time-format: yyyy-MM-dd HH:mm:ss
  current-time: ${current.time}

jwt:
  secret: "myTestSecretKeyThatIsLongEnoughForJWTHMACAlgorithmRequirements256Bits"
  access-token:
    expiration: 900   # 15 minutes = 900 seconds
  refresh-token:
    expiration: 3600  # 60 minutes = 3600 seconds
rate-limit:
  enabled: false
  max-attempts: 2
  window-seconds: 10